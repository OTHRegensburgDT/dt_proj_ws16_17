<UserControl x:Class="MotorXPGUIMVVM.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        xmlns:uc="clr-namespace:MotorXPGUIMVVM.UserControls"
        mc:Ignorable="d ignore"
        Background="WhiteSmoke"
        DataContext="{Binding MainView, Source={StaticResource Locator}}">

    <Grid HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
            <RowDefinition Height="300"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="600"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.Column="0" >
            <uc:GaugeControl DataContext="{Binding TempViewModel}"/>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="0">
            <uc:GaugeControl DataContext="{Binding SpeedViewModel}"/>
        </Grid>
        <Grid Grid.Row="0" Grid.Column="1" >
            <uc:LineChartControl/>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="1">
            <uc:LineChartControl/>
        </Grid>
    </Grid>
</UserControl>
